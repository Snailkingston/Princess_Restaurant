
  # Restaurant Website MVP
  ## Running the code


   open your terminal in the project directory and run the follwoing command :

   
 `npm i` to install the dependencies.

 `npm run dev` to start the development server.
  
